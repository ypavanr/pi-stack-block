backend:
   first run: 
      pip install requirements.txt
      to install dependencies
      then: 
      python init_db.py 
      to initialise the database
    


frontend:
   npm i
   npm run dev